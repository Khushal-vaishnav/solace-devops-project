apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solace-dc-pvc
  namespace: solace
spec:
  storageClassName: "gp2-immediate"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
